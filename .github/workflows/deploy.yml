name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Build & Deploy to GKE
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Set up Google Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          service_account_key: ${{ secrets.GCP_SA_KEY }}
          project_id: ${{ secrets.GCP_PROJECT_ID }}

      - name: Authenticate Docker to GCR
        run: |
          echo "${{ secrets.GCP_SA_KEY }}" > $HOME/gcloud-key.json
          gcloud auth activate-service-account --key-file=$HOME/gcloud-key.json
          gcloud auth configure-docker --quiet

      - name: Build & Push Docker Image
        run: |
          docker buildx create --use --platform linux/amd64
          docker buildx build --platform linux/amd64 \
            -t gcr.io/${{ secrets.GCP_PROJECT_ID }}/kubelink-api:latest \
            --push -f api/Dockerfile ./api

      - name: Get GKE Credentials
        run: |
          gcloud container clusters get-credentials ${{ secrets.GKE_CLUSTER_NAME }} --region ${{ secrets.GKE_CLUSTER_REGION }}

      - name: Deploy to GKE
        run: |
          kubectl apply -f k8s/
